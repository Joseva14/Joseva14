import {StyleSheet} from 'react-native'

/**
 * @param layoutStyle crea estilos de react-native
 */
const layoutStyle = StyleSheet.create({
    conatiner:{
        flex:1,
        justifyContent: 'center',
        padding: 20
    }
})

export default layoutStyle